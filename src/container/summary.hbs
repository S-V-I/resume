{{! 
Це код container файлу для summary сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
}}

<div>
    {{#with main}}
        <main>
            <section>
            
                {{> title data=summary.title big=true}}
            
            <div>
                <p>{{summary.text}}</p>
            </div>
            </section>

            {{#with experience}}
                <section>
                    <div>
                          {{> title data=title big=false}}
                    </div>
                    <div>
                        <p>{{text}}</p>
                    </div>
                </section>
            {{/with}}
        </main>
    {{/with}}
</div>

{{!-- щоб не повторювати башато разів "social" - використовуємо "#with"
    <a href={{social.email.href}}>{{social.email.text}}</a>
    /
    <a href={{social.phone.href}}>{{social.phone.text}}</a>
    /
    <a href={{social.linkedin.href}} target='_blank'>{{social.linkedin.text}}</a> --}}


    {{!-- це те саме, що:
       {{#with email}}
         {{> link href=href text=text}}
       {{/with}}

    або якщо без #with, то:
        {{> link href=email.href text=email.taxt}} 

    або так:
      {{#with phone}}
        {{> link}}
      {{/with}}

      {{#with linkedin}}
        {{> link}}
      {{/with}} --}}

{{!-- 
    Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
    Адже наш layout/default.hbs має вже структуру HTML
--}}